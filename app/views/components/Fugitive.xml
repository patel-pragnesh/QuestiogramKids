<Alloy>
    <!-- have to use alloy_id since I did not specify an id in the schema -->
	<TableViewRow layout="horizontal" backgroundColor="{bgColor}" modelId="{ItemID}">
		<View class="vgroup">
			<Label id="name" text="{name}"/>
			<Label id="address" text="{address}"/>
		</View>
	</TableViewRow>
</Alloy>